/** Static mountain still — hero background only (EN + ES home). */
export function HomeHeroBackdrop() {
  return (
    <div
      aria-hidden="true"
      className="home-hero-backdrop pointer-events-none absolute inset-0 -z-10"
    />
  )
}
