# Payload
PAYLOAD_SECRET=

# Postgres (Neon or Vercel Postgres)
DATABASE_URI=postgres://user:password@host:5432/lombera_law

# Vercel Blob (media storage)
BLOB_READ_WRITE_TOKEN=

# Set in Vercel project settings for preview/production; not needed locally
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Resend — staff email on contact form submit (required for notifications)
RESEND_API_KEY=
# Verified sender in Resend (use onboarding@resend.dev only for testing)
EMAIL_FROM=onboarding@resend.dev
EMAIL_FROM_NAME=Lombera Law
# Where new form leads are emailed (comma-separated ok for Resend "to")
CONTACT_NOTIFY_TO=
